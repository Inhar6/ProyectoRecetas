name: Integración Continua (Microservicios Recetas)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Código 📦
        uses: actions/checkout@v4

      # --- Configuración de Docker ---
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Build & Run Docker Compose 🐳
        # Usamos -f src/docker-compose.yml porque el archivo está dentro de la carpeta src
        run: docker compose -f src/docker-compose.yml up --build -d

      # --- Verificación de Salud y Estabilidad ---
      - name: Wait for Python Service Health Check (DB Connected) ⏳
        run: |
          echo "Esperando 25 segundos para que los contenedores se estabilicen..."
          sleep 25
          
          # IMPORTANTE: La frase de grep debe ser EXACTA a la que aparece en tu app.py
          if [ -z "$(docker compose -f src/docker-compose.yml logs ms-catalogo-py | grep "Tablas de PostgreSQL verificadas/creadas con éxito.")" ]; then
            echo "❌ ERROR: El Microservicio de Catálogo no inició correctamente o no conectó a la DB."
            docker compose -f src/docker-compose.yml logs ms-catalogo-py
            exit 1 
          else
            echo "✅ El Microservicio de Catálogo está conectado y listo."
          fi
          
      # --- Prueba de Integración (End-to-End) ---
      - name: Run Gateway Integration Test ✅
        run: |
          echo "Probando el endpoint principal del Gateway..."
          MAX_RETRIES=10
          RETRY_DELAY=5
          
          for i in $(seq 1 $MAX_RETRIES); do
            echo "Intento $i/$MAX_RETRIES: Probando http://localhost:8080/api/v1/recetas"
            if curl -s -f http://localhost:8080/api/v1/recetas; then
              echo "✅ El API Gateway respondió con éxito."
              exit 0 
            else
              echo "Esperando a que el Gateway esté listo..."
              sleep $RETRY_DELAY
            fi
          done
          
          echo "❌ ERROR FATAL: El API Gateway no respondió."
          docker compose -f src/docker-compose.yml logs api-gateway
          exit 1

      # --- Limpieza de Contenedores ---
      - name: Stop Containers 🛑
        if: always()
        run: docker compose -f src/docker-compose.yml down -v