name: Integración Continua (Microservicios Recetas)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Código 📦
        uses: actions/checkout@v4

      # --- Configuración de Docker ---
      # Esta acción es crítica; configura Docker y es suficiente para el comando 'docker compose'.
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      # === IMPORTANTE: Se elimina la acción 'docker/compose-action/setup' ===
      # La eliminamos para evitar conflictos de permisos con el daemon de Docker.

      - name: Build & Run Docker Compose 🐳
        # El comando 'docker compose' (sin guion) debe funcionar correctamente ahora.
        run: docker compose up --build -d

      # --- Verificación de Salud y Estabilidad ---
      - name: Wait for Python Service Health Check (DB Connected) ⏳
        run: |
          echo "Esperando 25 segundos para que los contenedores se estabilicen..."
          sleep 25
          
          if docker compose logs ms-catalogo-py | grep "Tablas de PostgreSQL verificadas/creadas con éxito."; then
            echo "✅ El Microservicio de Catálogo está conectado a PostgreSQL."
          else
            echo "❌ ERROR: El Microservicio de Catálogo falló al conectar a PostgreSQL. Revisar logs."
            docker compose logs ms-catalogo-py
            exit 1 
          fi
          
      # --- Prueba de Integración (End-to-End con Reintento) ---
      - name: Run Gateway Integration Test (Robust Retry Logic) ✅
        # Utiliza un bucle de reintento para ser robusto ante pequeños retrasos de arranque.
        run: |
          echo "Probando el endpoint principal del Gateway con reintentos..."
          MAX_RETRIES=10
          RETRY_DELAY=5
          
          for i in $(seq 1 $MAX_RETRIES); do
            echo "Intento $i/$MAX_RETRIES: Probando http://localhost:8080/api/v1/recetas"
            
            if curl -s -f http://localhost:8080/api/v1/recetas; then
              echo "✅ El API Gateway respondió con éxito."
              exit 0 
            else
              echo "Conexión rechazada o error HTTP. Esperando ${RETRY_DELAY} segundos..."
              sleep $RETRY_DELAY
            fi
          done
          
          echo "❌ ERROR FATAL: El API Gateway no respondió después de $MAX_RETRIES intentos."
          exit 1 

      # --- Limpieza ---
      - name: Cleanup Containers
        if: always() 
        run: docker compose down -v